# lucide-react-native over-declares react-dom as a peer; without this flag
# `npm ci` (which EAS Build runs verbatim) refuses the lock file generated
# with --legacy-peer-deps ("Missing: react-dom@… from lock file").
legacy-peer-deps=true
