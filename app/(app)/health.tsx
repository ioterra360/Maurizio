import { ScreenStub } from "../../components/ScreenStub";

export default function HealthScreen() {
  return (
    <ScreenStub
      title="Memory Health"
      note="Phase 2: retention ring, Stable / At-risk counters, cognitive load 3-zone bar, and the 2×2 folder grid with High / Medium / Low health chips."
    />
  );
}
