import { ScreenStub } from "../../components/ScreenStub";

export default function KnowledgeScreen() {
  return (
    <ScreenStub
      title="Knowledge"
      note="Phase 2: list of folders (Japanese · Medicine · Spanish · Law) with priority chip, retention bar inline, and a FAB to open Add to memory. Drag to reorder priority."
    />
  );
}
