# Core Loop Vero — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** chiudere il core loop di Memika — aggiungi un ricordo vero su Supabase, lo ripassi al momento giusto con mazzi veri dimensionati dal budget tempo, in un flusso fluido che finisce in un recap animato della mascotte; cartelle con statistiche/pausa/eliminazione, Progressi con dati veri.

**Architecture:** Approccio A della spec (`docs/superpowers/specs/2026-07-25-core-loop-design.md`): tutte le letture/scritture passano da `lib/api.ts` (unico chokepoint Supabase, branch demo esplicito in ogni funzione); la logica pura di budget/mapping vive in `lib/queue.ts` (testabile con vitest); il review-store carica i mazzi dal DB e traccia gli esiti per carta.

**Tech Stack:** Expo SDK 54, RN 0.81, TypeScript strict, NativeWind v4, zustand 5, Supabase, Reanimated v4, vitest (già configurato).

## Global Constraints

- **Expo SDK 54 only** — niente bump di pacchetti; nessuna dipendenza nuova in questo piano.
- **TypeScript strict, no `any`** — cast stretti solo al boundary.
- **Ogni accesso Supabase passa da `lib/api.ts`** con branch `if (isDemoMode)` esplicito; i componenti non importano supabase-js.
- **Mai editare migrazioni esistenti** — solo `npx supabase migration new <nome>`.
- **Ordine livelli bloccato**: Scan → Reinforcement → Focus, in copy e navigazione.
- **Font**: nomi famiglia letterali (`FONT.*` da `@/theme/tokens`).
- **Commit**: Conventional Commits con scope + trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`, messaggio via HEREDOC. Push su `main` (workflow del repo).
- **Dopo ogni task**: `npm run lint` (tsc) deve uscire 0; se il task ha test, `npm test` verde.
- **Demo mode invariata**: ogni superficie deve continuare a funzionare con `EXPO_PUBLIC_DEMO_MODE=true` (mazzi statici, no-op sulle scritture).
- Copy utente in italiano, tono calmo/editoriale (mai colpevolizzante).

---

### Task 1: Migrazione `folders.paused` + mapper

**Files:**
- Create: `supabase/migrations/<timestamp>_add_folders_paused.sql` (generata dal CLI)
- Modify: `lib/mappers.ts` (FolderRow/Folder/mapFolder)
- Modify: `lib/api.ts` (`fetchFolders` demo branch)
- Modify: `docs/DATA-MODEL.md` (documentare la colonna)

**Interfaces:**
- Produces: colonna `public.folders.paused boolean not null default false`; `Folder.paused: boolean` disponibile ovunque arrivi un Folder/FolderWithStats.

- [ ] **Step 1: Genera la migrazione**

Run: `npx supabase migration new add_folders_paused`
Poi scrivi nel file generato sotto `supabase/migrations/`:

```sql
-- Folder "dormiente": escluso dalla coda dei ripassi finché non riattivato.
-- Vedi docs/superpowers/specs/2026-07-25-core-loop-design.md §9.
alter table public.folders
  add column paused boolean not null default false;

comment on column public.folders.paused is
  'Paused folders are excluded from the review queue and Today counts.';
```

- [ ] **Step 2: Applica al DB remoto**

Run: `npx supabase db push`
Expected: la migrazione viene applicata senza errori.

- [ ] **Step 3: Aggiorna i mapper**

In `lib/mappers.ts`, aggiungi a `FolderRow`: `paused: boolean;` (dopo `icon`), a `Folder`: `paused: boolean;`, e in `mapFolder`: `paused: row.paused,`.

- [ ] **Step 4: Aggiorna il branch demo di `fetchFolders`**

In `lib/api.ts` (branch `isDemoMode` di `fetchFolders`), aggiungi `paused: false,` all'oggetto restituito. Fai lo stesso in `seedToFolderWithStats` (lib/api.ts ~riga 163) se tsc lo richiede.

- [ ] **Step 5: Lint + aggiorna DATA-MODEL.md + commit**

Run: `npm run lint` → exit 0.
In `docs/DATA-MODEL.md`, tabella folders: riga `| paused | boolean | Escluso dalla coda ripassi, default false |`.

```bash
git add supabase/migrations lib/mappers.ts lib/api.ts docs/DATA-MODEL.md
git commit -m "feat(db): add folders.paused for dormant folders"
```
(trailer Co-Authored-By come da Global Constraints — vale per tutti i commit del piano)

---

### Task 2: `lib/queue.ts` puro + test (TDD)

**Files:**
- Create: `lib/queue.ts`
- Test: `lib/queue.test.ts`

**Interfaces:**
- Consumes: `Memory` da `@/lib/mappers`, `ReviewCard` da `@/lib/review-store` (solo type import), `LayerKey` da `@/theme/tokens`.
- Produces (usati dai task 4 e 10):
  - `type LayerCounts = { scan: number; reinforcement: number; focus: number }`
  - `splitBudget(counts: LayerCounts, capTotal: number): LayerCounts`
  - `SECONDS_PER_ITEM: Record<LayerKey, number>` (scan 20, reinforcement 35, focus 40)
  - `layerMinutes(layer: LayerKey, items: number): number` (ceil ai minuti, min 1 se items>0)
  - `totalMinutes(counts: LayerCounts): number`
  - `toReviewCard(m: Memory, folderName: string, folderKind?: FolderKind): ReviewCard`
  - `DEMO_DUE_COUNTS: LayerCounts` = `{ scan: 4, reinforcement: 3, focus: 3 }` (dimensioni dei mazzi demo)

- [ ] **Step 1: Scrivi i test (falliranno)**

```ts
// lib/queue.test.ts
import { describe, expect, it } from "vitest";
import {
  DEMO_DUE_COUNTS, layerMinutes, splitBudget, toReviewCard, totalMinutes,
} from "./queue";
import type { Memory } from "./mappers";

const mem = (over: Partial<Memory> = {}): Memory => ({
  id: "m1", userId: "u1", folderId: "f1", term: "biblioteca",
  reading: null, definition: "Library", example: null, itemType: "word",
  state: "active",
  srs: { intervalDays: 1, easeFactor: 2.5, repetitions: 0 },
  lastReviewedAt: null, nextReviewAt: "2026-07-25T08:00:00.000Z",
  createdAt: "2026-07-20T08:00:00.000Z", updatedAt: "2026-07-20T08:00:00.000Z",
  ...over,
});

describe("splitBudget", () => {
  it("returns everything when under cap", () => {
    expect(splitBudget({ scan: 3, reinforcement: 2, focus: 1 }, 28))
      .toEqual({ scan: 3, reinforcement: 2, focus: 1 });
  });
  it("splits proportionally with remainder to scan first", () => {
    // totale 20, cap 8 → quote floor: scan 4, reinf 2, focus 1 (somma 7), resto 1 → scan
    expect(splitBudget({ scan: 10, reinforcement: 6, focus: 4 }, 8))
      .toEqual({ scan: 5, reinforcement: 2, focus: 1 });
  });
  it("never allocates beyond a layer's own queue", () => {
    const r = splitBudget({ scan: 1, reinforcement: 0, focus: 30 }, 8);
    expect(r.scan).toBe(1);
    expect(r.reinforcement).toBe(0);
    expect(r.scan + r.reinforcement + r.focus).toBe(8);
  });
  it("handles empty queue", () => {
    expect(splitBudget({ scan: 0, reinforcement: 0, focus: 0 }, 28))
      .toEqual({ scan: 0, reinforcement: 0, focus: 0 });
  });
});

describe("minutes", () => {
  it("estimates per layer (ceil, min 1 when non-empty)", () => {
    expect(layerMinutes("scan", 0)).toBe(0);
    expect(layerMinutes("scan", 1)).toBe(1);
    expect(layerMinutes("focus", 6)).toBe(4); // 240s
  });
  it("totals the three layers", () => {
    expect(totalMinutes({ scan: 3, reinforcement: 0, focus: 0 })).toBe(1);
  });
});

describe("toReviewCard", () => {
  it("maps a Memory including the composed SRS snapshot", () => {
    const c = toReviewCard(mem(), "Spanish", "es");
    expect(c).toMatchObject({
      id: "m1", front: "biblioteca", back: "Library",
      folder: "Spanish", folderKind: "es",
    });
    expect(c.srs).toEqual({
      intervalDays: 1, easeFactor: 2.5, repetitions: 0,
      nextReviewAt: "2026-07-25T08:00:00.000Z", lastReviewedAt: null,
    });
  });
  it("falls back to example as hint and omits empties", () => {
    const c = toReviewCard(mem({ example: "¡Voy a la biblioteca!" }), "Spanish");
    expect(c.hint).toBe("¡Voy a la biblioteca!");
    expect(toReviewCard(mem(), "Spanish").hint).toBeUndefined();
    expect(toReviewCard(mem(), "Spanish").reading).toBeUndefined();
  });
});

describe("DEMO_DUE_COUNTS", () => {
  it("mirrors the static demo deck sizes", () => {
    expect(DEMO_DUE_COUNTS).toEqual({ scan: 4, reinforcement: 3, focus: 3 });
  });
});
```

- [ ] **Step 2: Verifica che falliscano**

Run: `npm test -- lib/queue.test.ts`
Expected: FAIL — `Cannot find module './queue'`.

- [ ] **Step 3: Implementa `lib/queue.ts`**

```ts
/**
 * Logica pura della coda di ripasso: ripartizione del budget tempo sui tre
 * livelli e mapping Memory → ReviewCard. Nessun I/O — testato con vitest.
 */
import type { LayerKey } from "@/theme/tokens";
import type { Memory } from "./mappers";
import type { ReviewCard } from "./review-store";
import type { FolderKind } from "./constants";

export type LayerCounts = { scan: number; reinforcement: number; focus: number };

const LAYER_ORDER: readonly LayerKey[] = ["scan", "reinforcement", "focus"];

/** Dimensioni dei mazzi demo statici (review-store DECKS) per i conteggi offline. */
export const DEMO_DUE_COUNTS: LayerCounts = { scan: 4, reinforcement: 3, focus: 3 };

/** Secondi stimati per carta, per livello. Euristica dichiarata — da tarare. */
export const SECONDS_PER_ITEM: Record<LayerKey, number> = {
  scan: 20,
  reinforcement: 35,
  focus: 40,
};

/**
 * Ripartisce il tetto `capTotal` sui tre livelli in proporzione alla coda,
 * senza mai superare la coda del singolo livello. Il resto della divisione
 * va ai livelli con domanda residua in ordine Scan → Reinforcement → Focus.
 */
export function splitBudget(counts: LayerCounts, capTotal: number): LayerCounts {
  const total = counts.scan + counts.reinforcement + counts.focus;
  if (total <= capTotal) return { ...counts };
  const out: LayerCounts = { scan: 0, reinforcement: 0, focus: 0 };
  for (const l of LAYER_ORDER) {
    out[l] = Math.min(counts[l], Math.floor((capTotal * counts[l]) / total));
  }
  let rest = capTotal - (out.scan + out.reinforcement + out.focus);
  while (rest > 0) {
    let gave = false;
    for (const l of LAYER_ORDER) {
      if (rest > 0 && out[l] < counts[l]) {
        out[l] += 1;
        rest -= 1;
        gave = true;
      }
    }
    if (!gave) break; // ogni livello è saturo
  }
  return out;
}

export function layerMinutes(layer: LayerKey, items: number): number {
  if (items <= 0) return 0;
  return Math.max(1, Math.ceil((items * SECONDS_PER_ITEM[layer]) / 60));
}

export function totalMinutes(counts: LayerCounts): number {
  return (
    layerMinutes("scan", counts.scan) +
    layerMinutes("reinforcement", counts.reinforcement) +
    layerMinutes("focus", counts.focus)
  );
}

/**
 * Memory (modello DB) → ReviewCard (modello dei tre screen di ripasso).
 * `hint` non esiste a DB: fallback = frase d'esempio, se c'è. Il snapshot
 * SRS ricompone i campi che vivono FUORI da memory.srs (vedi nota in
 * review-store.ReviewCard).
 */
export function toReviewCard(
  m: Memory,
  folderName: string,
  folderKind?: FolderKind,
): ReviewCard {
  return {
    id: m.id,
    front: m.term,
    reading: m.reading ?? undefined,
    back: m.definition,
    example: m.example ?? undefined,
    hint: m.example ?? undefined,
    folder: folderName,
    folderKind,
    srs: { ...m.srs, nextReviewAt: m.nextReviewAt, lastReviewedAt: m.lastReviewedAt },
  };
}
```

- [ ] **Step 4: Test verdi + lint**

Run: `npm test -- lib/queue.test.ts` → PASS (tutti). Run: `npm run lint` → exit 0.

- [ ] **Step 5: Commit**

```bash
git add lib/queue.ts lib/queue.test.ts
git commit -m "feat(srs): pure queue module — budget split, minute estimates, Memory->ReviewCard"
```

---

### Task 3: API — createMemory, conteggi, filtri cartella/pausa, pausa/elimina cartella

**Files:**
- Modify: `lib/api.ts`

**Interfaces:**
- Consumes: mapper esistenti, `isDemoMode`, `supabase` (già importati in api.ts), `DEMO_DUE_COUNTS` e `LayerCounts` da `@/lib/queue`.
- Produces (firme esatte per i task 4, 9, 10, 12):
  - `createMemory(input: { userId: string; folderId: string; term: string; reading?: string; definition: string; example?: string; itemType?: string }): Promise<Memory | null>` (demo → null)
  - `fetchTodayInputCount(userId: string): Promise<number>` (demo → 0)
  - `fetchDueCounts(userId: string, folderId?: string): Promise<LayerCounts>` (demo → `DEMO_DUE_COUNTS`)
  - `fetchDueMemoriesByLayer(userId, layer, opts?: { folderId?: string; limit?: number }): Promise<Memory[]>` — **firma cambiata** (era `limit = 30` posizionale; nessun altro call-site esiste oggi: verificare con grep)
  - `updateFolderPaused(folderId: string, paused: boolean): Promise<void>`
  - `deleteFolder(folderId: string): Promise<void>`
  - `countMemoriesInFolder(folderId: string): Promise<number>` (demo → 0)

- [ ] **Step 1: Helper cartelle in pausa + range del giorno locale**

Aggiungi in `lib/api.ts` (sezione Folders):

```ts
/** Ids delle cartelle in pausa dell'utente — usati per escluderle dalla coda. */
async function pausedFolderIds(userId: string): Promise<string[]> {
  const { data, error } = await supabase
    .from("folders")
    .select("id")
    .eq("user_id", userId)
    .eq("paused", true);
  if (error) throw error;
  return (data ?? []).map((r: { id: string }) => r.id);
}

/** Range ISO [mezzanotte locale, ora] per il conteggio inserimenti di oggi. */
function todayRange(): { from: string; to: string } {
  const now = new Date();
  const from = new Date(now.getFullYear(), now.getMonth(), now.getDate());
  return { from: from.toISOString(), to: now.toISOString() };
}
```

- [ ] **Step 2: `createMemory` + `fetchTodayInputCount` + `countMemoriesInFolder`**

Dopo `fetchMemoriesForFolder`:

```ts
/**
 * Crea un ricordo. I default SRS vivono nello schema (next_review_at =
 * now(): entra subito in coda — il toast "domani" è framing UX, SRS.md).
 * Demo: no-op → null.
 */
export async function createMemory(input: {
  userId: string;
  folderId: string;
  term: string;
  reading?: string;
  definition: string;
  example?: string;
  itemType?: string;
}): Promise<Memory | null> {
  if (isDemoMode) return null;
  const { data, error } = await supabase
    .from("memories")
    .insert({
      user_id: input.userId,
      folder_id: input.folderId,
      term: input.term,
      reading: input.reading ?? null,
      definition: input.definition,
      example: input.example ?? null,
      item_type: input.itemType ?? null,
    })
    .select("*")
    .single<MemoryRow>();
  if (error) throw error;
  return mapMemory(data);
}

/** Ricordi inseriti oggi (giorno locale) — per il limite giornaliero. */
export async function fetchTodayInputCount(userId: string): Promise<number> {
  if (isDemoMode) return 0;
  const { from, to } = todayRange();
  const { count, error } = await supabase
    .from("memories")
    .select("id", { count: "exact", head: true })
    .eq("user_id", userId)
    .gte("created_at", from)
    .lte("created_at", to);
  if (error) throw error;
  return count ?? 0;
}

/** Quanti ricordi contiene una cartella — per la conferma di eliminazione. */
export async function countMemoriesInFolder(folderId: string): Promise<number> {
  if (isDemoMode) return 0;
  const { count, error } = await supabase
    .from("memories")
    .select("id", { count: "exact", head: true })
    .eq("folder_id", folderId);
  if (error) throw error;
  return count ?? 0;
}
```

- [ ] **Step 3: filtro cartella + esclusione pausa in `fetchDueMemoriesByLayer`**

Sostituisci la firma e il corpo iniziale (mantieni i predicati per livello identici):

```ts
export async function fetchDueMemoriesByLayer(
  userId: string,
  layer: LayerKey,
  opts: { folderId?: string; limit?: number } = {},
): Promise<Memory[]> {
  if (isDemoMode) return [];
  const limit = opts.limit ?? 30;
  const nowIso = new Date().toISOString();
  let query = supabase
    .from("memories")
    .select("*")
    .eq("user_id", userId)
    .neq("state", "archived")
    .lte("next_review_at", nowIso);
  if (opts.folderId) {
    query = query.eq("folder_id", opts.folderId);
  } else {
    // Le cartelle in pausa escono dalla coda globale. Una sessione scoped
    // (folderId esplicito) invece le può ripassare comunque.
    const paused = await pausedFolderIds(userId);
    if (paused.length > 0) query = query.not("folder_id", "in", `(${paused.join(",")})`);
  }
  // ... predicati per livello INVARIATI (scan/reinforcement/focus) ...
  // ... order/limit/returns INVARIATI, con `limit` della nuova firma ...
}
```

- [ ] **Step 4: `fetchDueCounts`**

```ts
/**
 * Conteggi della coda per livello — per il piano reattivo di Oggi e per la
 * ripartizione del budget. Demo: dimensioni dei mazzi statici.
 */
export async function fetchDueCounts(
  userId: string,
  folderId?: string,
): Promise<LayerCounts> {
  if (isDemoMode) return { ...DEMO_DUE_COUNTS };
  const nowIso = new Date().toISOString();
  const paused = folderId ? [] : await pausedFolderIds(userId);
  const base = () => {
    let q = supabase
      .from("memories")
      .select("id", { count: "exact", head: true })
      .eq("user_id", userId)
      .neq("state", "archived")
      .lte("next_review_at", nowIso);
    if (folderId) q = q.eq("folder_id", folderId);
    else if (paused.length > 0) q = q.not("folder_id", "in", `(${paused.join(",")})`);
    return q;
  };
  const [scan, reinforcement, focus] = await Promise.all([
    base().lt("srs_repetitions", 3).neq("state", "fading"),
    base().or("and(srs_repetitions.gte.3,srs_repetitions.lt.8),state.eq.fading"),
    base().gte("srs_repetitions", 8).neq("state", "fading"),
  ].map(async (q) => {
    const { count, error } = await q;
    if (error) throw error;
    return count ?? 0;
  }));
  return { scan, reinforcement, focus };
}
```

Import in testa a api.ts: `import { DEMO_DUE_COUNTS, type LayerCounts } from "./queue";`

- [ ] **Step 5: pausa + eliminazione cartella**

Accanto a `updateFolderName`:

```ts
/** Attiva/disattiva la pausa di una cartella. Demo no-op. */
export async function updateFolderPaused(folderId: string, paused: boolean): Promise<void> {
  if (isDemoMode) return;
  const { error } = await supabase.from("folders").update({ paused }).eq("id", folderId);
  if (error) throw error;
}

/** Elimina la cartella. I ricordi cascano a DB (on delete cascade). Demo no-op. */
export async function deleteFolder(folderId: string): Promise<void> {
  if (isDemoMode) return;
  const { error } = await supabase.from("folders").delete().eq("id", folderId);
  if (error) throw error;
}
```

- [ ] **Step 6: Verifica call-site + lint + commit**

Run: `grep -rn "fetchDueMemoriesByLayer" app lib features` → nessun call-site col terzo argomento numerico.
Run: `npm run lint` → exit 0. `npm test` → verde.

```bash
git add lib/api.ts
git commit -m "feat(api): createMemory, due counts, folder/paused filters, folder pause+delete"
```

---

### Task 4: Review-store — mazzi veri, esiti per carta, correzione flash

**Files:**
- Modify: `lib/review-store.ts`
- Modify: `app/(app)/today.tsx` (solo i call-site di `start`)
- Modify: `app/(app)/folder/[kind].tsx` (call-site di `start` con `folderId`)

**Interfaces:**
- Consumes: `fetchDueMemoriesByLayer`, `fetchDueCounts` (Task 3); `splitBudget`, `toReviewCard`, `type LayerCounts` (Task 2); `isDemoMode` da `./supabase`; `fetchFolders` per i nomi cartella.
- Produces (per i task 5, 7, 10):
  - `start(layer, mode, opts?: { folderKind?: FolderKind; folderId?: string; budgetCap?: number }): void` — **firma cambiata** (era `folderKind` posizionale)
  - stato nuovo: `deck: ReviewCard[] | null`, `deckLoading: boolean`, `layerCaps: LayerCounts | null`, `results: RecapEntry[]`
  - `type RecapEntry = { id: string; term: string; reading?: string; layer: LayerKey; response: "remembered" | "struggled" | "forgot"; revealed: boolean }` (esportato)
  - `recordAndAdvance(response, opts?: { revealed?: boolean }): "next" | "handoff" | "done"` — **firma estesa**
  - `amendLastAnswer(): boolean` — corregge l'ultima risposta Scan in `struggled` entro la finestra flash; ritorna false se la finestra è chiusa
  - `AMEND_WINDOW_MS = 1400` (esportata — usata dal flash UI)
  - `cards()` ritorna `deck ?? mazzoDemo` (demo invariata)

- [ ] **Step 1: Stato e caricamento mazzo**

In `review-store.ts`:
1. Import: `fetchDueMemoriesByLayer, fetchDueCounts, fetchFolders` da `./api`; `isDemoMode` da `./supabase`; `splitBudget, toReviewCard, type LayerCounts` da `./queue`.
2. Aggiungi a `ReviewState`: `deck: ReviewCard[] | null; deckLoading: boolean; layerCaps: LayerCounts | null; folderId: string | null; budgetCap: number | null; results: RecapEntry[];` e le firme di `start`/`recordAndAdvance`/`amendLastAnswer` come sopra.
3. Nuova funzione modulo (accanto a `openSessionFor`):

```ts
/** Sequenza monotona per i load del mazzo — l'ultimo vince. */
let deckLoadSeq = 0;

async function loadDeckFor(
  layer: LayerKey,
  set: (p: Partial<ReviewState>) => void,
  get: () => ReviewState,
): Promise<void> {
  const userId = useAuthStore.getState().user?.id;
  if (isDemoMode || !userId) {
    // Demo/anonimo: i mazzi statici restano la fonte (deck=null → fallback).
    set({ deck: null, deckLoading: false });
    return;
  }
  const myId = ++deckLoadSeq;
  set({ deckLoading: true });
  try {
    const s = get();
    const cap = s.layerCaps?.[layer] ?? s.budgetCap ?? 28;
    const [memories, folders] = await Promise.all([
      fetchDueMemoriesByLayer(userId, layer, {
        folderId: s.folderId ?? undefined,
        limit: Math.max(cap, 0),
      }),
      fetchFolders(userId),
    ]);
    if (myId !== deckLoadSeq) return;
    const nameById = new Map(folders.map((f) => [f.id, f.name]));
    const kindById = new Map(folders.map((f) => [f.id, f.kind]));
    set({
      deck: memories.map((m) =>
        toReviewCard(m, nameById.get(m.folderId) ?? "Memika",
          kindById.get(m.folderId) as ReviewCard["folderKind"]),
      ),
      deckLoading: false,
    });
  } catch (e) {
    if (myId === deckLoadSeq) set({ deck: [], deckLoading: false });
    if (__DEV__) console.warn("[review] deck load failed", e);
  }
}
```

- [ ] **Step 2: `start` con opts + ripartizione budget in flow**

Sostituisci `start` (mantieni il reset esistente, aggiungi i campi nuovi):

```ts
start: (layer, mode, opts = {}) => {
  lastRecordAt = 0;
  clearPendingScanPersist();
  set({
    layer, mode,
    folderKind: opts.folderKind ?? null,
    folderId: opts.folderId ?? null,
    budgetCap: opts.budgetCap ?? null,
    layerCaps: null,
    deck: null, deckLoading: !isDemoMode,
    results: [],
    index: 0, totals: EMPTY_COUNTS, layerTotals: EMPTY_COUNTS,
    sessionId: null, pendingSessionLayer: null,
    pendingItems: [], pendingSessionComplete: null,
  });
  openSessionFor(layer, set, get);
  const userId = useAuthStore.getState().user?.id;
  if (mode === "flow" && !isDemoMode && userId && opts.budgetCap) {
    // Il piano fluido ripartisce il budget sui tre livelli PRIMA del load,
    // così ogni advanceToLayer trova il suo tetto già calcolato.
    void fetchDueCounts(userId)
      .then((counts) => set({ layerCaps: splitBudget(counts, opts.budgetCap!) }))
      .catch(() => set({ layerCaps: null }))
      .finally(() => void loadDeckFor(layer, set, get));
  } else {
    void loadDeckFor(layer, set, get);
  }
},
```

`advanceToLayer` e `ensureSession`: dopo il rispettivo `openSessionFor(...)`, aggiungi `set({ deck: null, deckLoading: !isDemoMode }); void loadDeckFor(next /* o layer */, set, get);` (in `advanceToLayer` NON azzerare `results`, `layerCaps`, `folderId` — nota: rimuovi `folderKind: null` dal set esistente di `advanceToLayer` SOLO se la sessione è flow scoped; oggi flow parte sempre non-scoped, quindi lascialo). `ensureSession` azzera anche `results: []`.

- [ ] **Step 3: `cards()` col mazzo vero + fallback demo**

```ts
cards: () => {
  const s = get();
  if (s.deck) return s.deck;
  return deckFor(s); // demo statico — identico a prima
},
current: () => get().cards()[get().index],
```

ATTENZIONE (bug zustand v5 già noto): gli screen NON chiamano `s.cards()` nel selettore — selezionano `deck`/`layer`/`folderKind` e memoizzano (già così dal fix del 2026-07-25; il Task 5 aggiorna il memo per includere `deck`).

- [ ] **Step 4: Esiti per carta + persistenza differita dello Scan + amend**

1. Helper modulo:

```ts
export const AMEND_WINDOW_MS = 1400;

/** Persistenza Scan differita per la finestra di correzione del flash. */
let pendingScanPersist: {
  timer: ReturnType<typeof setTimeout>;
  run: () => void; // esegue subito la persistenza (flush)
} | null = null;

function clearPendingScanPersist(flush = true) {
  if (!pendingScanPersist) return;
  clearTimeout(pendingScanPersist.timer);
  const p = pendingScanPersist;
  pendingScanPersist = null;
  if (flush) p.run();
}
```

2. In `recordAndAdvance(response, opts = {})`: dopo il calcolo di `totals`/`layerTotals`, aggiungi l'esito:

```ts
const entry: RecapEntry = {
  id: card.id, term: card.front, reading: card.reading,
  layer: state.layer, response, revealed: opts.revealed ?? false,
};
set({ results: [...state.results, entry] });
```

3. Persistenza: estrai il blocco `applyScheduledUpdate` + `recordReviewItem`/`pendingItems` in una closure `persist(finalResponse, finalSrs)`. Per `state.layer === "scan"`, invece di eseguirla subito: `clearPendingScanPersist(); pendingScanPersist = { timer: setTimeout(() => { pendingScanPersist = null; persist(response, updated); }, AMEND_WINDOW_MS), run: () => persist(response, updated) };` conservando anche `prior` (lo snapshot SRS pre-update) in una variabile modulo `lastScanAnswer = { cardId: card.id, prior, response }`. Per gli altri livelli, comportamento invariato (persist immediata).
4. `amendLastAnswer()`:

```ts
amendLastAnswer: () => {
  if (!pendingScanPersist || !lastScanAnswer) return false;
  clearTimeout(pendingScanPersist.timer);
  pendingScanPersist = null;
  const { cardId, prior } = lastScanAnswer;
  const s = get();
  const corrected = scheduleUpdate(prior, toLayerOutcome("scan", "struggled"));
  // remembered → struggled: sposta i contatori e correggi l'ultima entry.
  const fix = (c: Counts): Counts => ({
    ...c, remembered: c.remembered - 1, struggled: c.struggled + 1,
  });
  const results = s.results.slice();
  const last = results[results.length - 1];
  if (last && last.id === cardId) results[results.length - 1] = { ...last, response: "struggled" };
  set({
    totals: fix(s.totals), layerTotals: fix(s.layerTotals),
    srsByCard: { ...s.srsByCard, [cardId]: corrected },
    results,
  });
  // Persisti la versione corretta (stessa closure della risposta originale,
  // con outcome corretto).
  persistAmended(cardId, corrected);
  lastScanAnswer = null;
  return true;
},
```

dove `persistAmended` riusa `applyScheduledUpdate(cardId, corrected)` + `recordReviewItem(...response:"struggled"...)` con le stesse guardie (`isPersistableMemoryId`, sessionId/pendingItems) del percorso normale.

5. `reset()` e `advanceToLayer()`: chiama `clearPendingScanPersist(true)` in testa (flush della risposta org.); `reset` azzera anche `deck: null, deckLoading: false, layerCaps: null, folderId: null, budgetCap: null, results: []`.

- [ ] **Step 5: call-site**

- `app/(app)/today.tsx`: `startSession("scan", "flow")` → `startSession("scan", "flow", { budgetCap: TIME_BUDGETS[budgetIndex].estItems })` (il Task 10 definisce `budgetIndex`; per ora passa `{ budgetCap: 28 }` con TODO rimosso nel Task 10) e `startSession(path, "single")` → `startSession(path, "single", { budgetCap: 28 })`.
- `app/(app)/folder/[kind].tsx` `startReview`: `startSession("scan", "single", { folderKind: kind, folderId: data.id, budgetCap: 28 })`.

- [ ] **Step 6: Lint + commit**

Run: `npm run lint` → exit 0. `npm test` → verde.

```bash
git add lib/review-store.ts "app/(app)/today.tsx" "app/(app)/folder/[kind].tsx"
git commit -m "feat(srs): real decks from Supabase, per-card results, deferred scan persistence with amend"
```

---

### Task 5: Scan — flash di conferma con correzione + MascotLoader hook-in

**Files:**
- Modify: `app/review/scan.tsx`
- Modify: `app/review/reinforcement.tsx` (deck memo + hint fallback)
- Modify: `app/review/focus.tsx` (deck memo)

**Interfaces:**
- Consumes: `amendLastAnswer`, `AMEND_WINDOW_MS`, `deckLoading`, `deck`, `recordAndAdvance(response, { revealed })` (Task 4). `MascotLoader` arriva nel Task 8: fino ad allora usa `ActivityIndicator`, il Task 8 lo sostituisce.

- [ ] **Step 1: memo del mazzo aggiornato (tutti e tre gli screen)**

Nei tre screen, il memo esistente diventa (aggiungi `deck` e `deckLoading` alle selezioni):

```ts
const deck = useReviewStore((s) => s.deck);
const deckLoading = useReviewStore((s) => s.deckLoading);
const layer = useReviewStore((s) => s.layer);
const folderKind = useReviewStore((s) => s.folderKind);
const getCards = useReviewStore((s) => s.cards);
const cards = useMemo(() => getCards(), [getCards, layer, folderKind, deck]);
```

e prima del render della carta: `if (deckLoading) return (<SafeAreaView className="flex-1 bg-warm-white" edges={["top"]}><View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}><ActivityIndicator color={colors.navy} /></View></SafeAreaView>);`

- [ ] **Step 2: flash di conferma in scan.tsx**

Stato: `const [flash, setFlash] = useState<null | { back: string; amended: boolean; result: "next" | "handoff" | "done" }>(null);` e un ref timer `const flashTimer = useRef<ReturnType<typeof setTimeout> | null>(null);`.

`handleRemember` diventa:

```ts
const handleRemember = () => {
  const response = revealed ? "struggled" : "remembered";
  if (revealed) tap();
  else success();
  const back = card.back;
  const result = recordAndAdvance(response, { revealed });
  if (!revealed) {
    // Flash di conferma: mostra la soluzione per la finestra di correzione,
    // poi prosegui (la navigazione di fine mazzo aspetta il flash).
    setFlash({ back, amended: false, result });
    flashTimer.current = setTimeout(() => {
      setFlash(null);
      routeAfter(result);
    }, AMEND_WINDOW_MS);
  } else {
    routeAfter(result);
  }
};

const routeAfter = (result: "next" | "handoff" | "done") => {
  if (result === "handoff") router.replace("/review/handoff");
  else if (result === "done") router.replace("/review/complete");
};

const handleAmend = () => {
  if (!flash || flash.amended) return;
  const ok = amendLastAnswer();
  if (ok) {
    tap();
    setFlash({ ...flash, amended: true });
  }
};
```

Cleanup: `useEffect(() => () => { if (flashTimer.current) clearTimeout(flashTimer.current); }, []);`

Overlay JSX (dopo il blocco `revealed`, dentro il container centrale):

```tsx
{flash ? (
  <Pressable
    onPress={handleAmend}
    accessibilityRole="button"
    accessibilityLabel="Non lo ricordavo — segna come difficile"
    style={{
      backgroundColor: flash.amended ? layerTint.reinforcement : layerTint.scanReveal,
      paddingHorizontal: 20, paddingVertical: 18, marginTop: 36,
      alignSelf: "stretch", borderRadius: 14,
    }}
  >
    <Text style={{ fontFamily: FONT.medium, fontSize: 19, color: colors.navy, lineHeight: 26 }}>
      {flash.back}
    </Text>
    <Text style={{ marginTop: 8, fontFamily: FONT.regular, fontSize: 12.5, color: colors.midGrey }}>
      {flash.amended ? "Segnato come difficile" : "Sbagliato? Tocca per segnarlo come difficile"}
    </Text>
  </Pressable>
) : null}
```

(Se `layerTint.reinforcement` non esiste nei token, usa `layerTint.scanReveal` per entrambi gli stati e differenzia col testo.) Mentre `flash` è attivo, disabilita i bottoni azione (`disabled={!!flash}` su entrambi i Tappable).

- [ ] **Step 3: hint fallback in reinforcement.tsx**

Le carte vere possono non avere `hint`. Dove lo screen avanza `pre → hint`, sostituisci con `setStage(card.hint ? "hint" : "answer")`; nel render dello stage hint mostra `card.hint` (ora garantito). Verifica leggendo il file per gli anchor esatti.

- [ ] **Step 4: Lint + commit**

Run: `npm run lint` → exit 0.

```bash
git add app/review/scan.tsx app/review/reinforcement.tsx app/review/focus.tsx
git commit -m "feat(review): scan confirmation flash with amend window, deck loading states, hint fallback"
```

---

### Task 6: Interstitial automatico tra i livelli

**Files:**
- Modify: `app/review/handoff.tsx`

**Interfaces:**
- Consumes: `advanceToLayer` (invariato). Nessun consumer a valle.

- [ ] **Step 1: auto-advance**

In `handoff.tsx`: rimuovi `LayerCard`, `PrimaryButton`, `deckSizeFor` (import e JSX). Dopo la definizione di `goNext`, aggiungi:

```ts
// Interstitial automatico: nessun tap tra i livelli (spec §5). goNext è
// idempotente via ranRef, quindi un unmount anticipato non double-fire.
useEffect(() => {
  const t = setTimeout(goNext, 1100);
  return () => clearTimeout(t);
}, [goNext]);
```

Il JSX restante: mascotte + "{finished} completato" + "Avanti con {next.label}" + sottotitolo esistente. Aggiungi in fondo (sotto il sottotitolo) tre puntini di progresso col colore del livello in arrivo per dare il senso di transizione:

```tsx
<View style={{ flexDirection: "row", gap: 6, marginTop: 22 }}>
  {[0, 1, 2].map((i) => (
    <View key={i} style={{ width: 6, height: 6, borderRadius: 3, backgroundColor: next.color, opacity: 0.35 + i * 0.3 }} />
  ))}
</View>
```

- [ ] **Step 2: Lint + commit**

Run: `npm run lint` → exit 0.

```bash
git add app/review/handoff.tsx
git commit -m "feat(review): auto-advancing layer interstitial — fluid flow without taps"
```

---

### Task 7: Recap di fine sessione con mascotte animata

**Files:**
- Rewrite: `app/review/complete.tsx`

**Interfaces:**
- Consumes: `results: RecapEntry[]`, `totals`, `mode`, `reset` dal review-store; `Mascot`; Reanimated (`react-native-reanimated` già installato).
- Nota: **prima di scrivere il grafico, l'esecutore DEVE caricare la skill `dataviz`** e seguirne le regole di colore/forma.

- [ ] **Step 1: struttura dati recap**

In `complete.tsx` (rewrite completo del file):

```ts
const totals = useReviewStore((s) => s.totals);
const results = useReviewStore((s) => s.results);
const mode = useReviewStore((s) => s.mode);
const reset = useReviewStore((s) => s.reset);
const total = totals.reviewed || results.length;
const pct = total > 0 ? totals.remembered / total : 0;
const tier = pct >= 0.8 ? "top" : pct >= 0.5 ? "mid" : "low";
const TIER_COPY = {
  top: { title: "Sessione brillante!", body: "La tua memoria di lungo termine ringrazia. Torna domani per consolidare." },
  mid: { title: "Buon lavoro, si costruisce così.", body: "Qualche ricordo ha avuto bisogno di una mano — è esattamente ciò che serve all'algoritmo per aiutarti." },
  low: { title: "Giornata dura? Va benissimo.", body: "Dimenticare fa parte del processo: questi ricordi torneranno presto e saranno più leggeri." },
} as const;
// Breakdown per livello (solo flow): raggruppa results per entry.layer.
const byLayer = useMemo(() => {
  const acc: Record<string, { remembered: number; struggled: number; forgot: number }> = {};
  for (const r of results) {
    acc[r.layer] ??= { remembered: 0, struggled: 0, forgot: 0 };
    acc[r.layer][r.response] += 1;
  }
  return acc;
}, [results]);
```

- [ ] **Step 2: mascotte animata (Reanimated)**

```tsx
import Animated, {
  useAnimatedStyle, useSharedValue, withDelay, withRepeat, withSequence, withSpring, withTiming,
} from "react-native-reanimated";

function AnimatedMascot() {
  const enter = useSharedValue(0);
  const wobble = useSharedValue(0);
  useEffect(() => {
    enter.value = withSpring(1, { damping: 12, stiffness: 120 });
    wobble.value = withDelay(600, withRepeat(
      withSequence(withTiming(1, { duration: 1400 }), withTiming(-1, { duration: 1400 })),
      -1, true,
    ));
  }, [enter, wobble]);
  const style = useAnimatedStyle(() => ({
    opacity: enter.value,
    transform: [
      { scale: 0.6 + enter.value * 0.4 },
      { translateY: (1 - enter.value) * 24 },
      { rotate: `${wobble.value * 3}deg` },
    ],
  }));
  return (
    <Animated.View style={style}>
      <Mascot variant="checklist" size={150} withShadow={false} />
    </Animated.View>
  );
}
```

- [ ] **Step 3: grafico risultati + lista carte**

Grafico: barre orizzontali proporzionali (niente librerie — View con larghezza percentuale), una per esito, con etichetta e conteggio `tabular-nums`; colori dagli ink già usati (`statusTint.active.text` per Ricordati, `colors.navy` per Difficili, `statusTint.fading.text` per Dimenticati — già scelti nel vecchio complete per il contrasto WCAG). Applica le regole della skill dataviz (assi/na, label leggibili, niente decorazione).

Lista carte (dentro lo ScrollView del recap):

```tsx
{results.map((r) => (
  <View key={r.id + r.layer} className="flex-row items-center" style={{ paddingVertical: 8, gap: 10 }}>
    <View style={{ width: 8, height: 8, borderRadius: 4, backgroundColor: DOT[r.response] }} />
    <View style={{ flex: 1, minWidth: 0 }}>
      <Text numberOfLines={1} style={{ fontFamily: FONT.semibold, fontSize: 15, color: colors.navy }}>
        {r.term}{r.reading ? `  ·  ${r.reading}` : ""}
      </Text>
    </View>
    {r.revealed ? (
      <Text style={{ fontFamily: FONT.regular, fontSize: 11.5, color: colors.midGrey }}>Mostrami</Text>
    ) : null}
    <Text style={{ fontFamily: FONT.medium, fontSize: 12.5, color: DOT[r.response] }}>
      {LABEL[r.response]}
    </Text>
  </View>
))}
```

con `const DOT = { remembered: statusTint.active.text, struggled: colors.navy, forgot: statusTint.fading.text } as const;` e `const LABEL = { remembered: "Ricordato", struggled: "Difficile", forgot: "Dimenticato" } as const;`. In modalità flow, sopra la lista inserisci il breakdown per livello (tre righe compatte `Scan · N ricordati · N difficili · N dimenticati` col colore del livello da `layerTokens`). Se `results.length === 0` (sessione vuota), mostra solo mascotte + "Niente da ripassare — torna più tardi." e CTA.

Titolo schermata: `TIER_COPY[tier].title`; sottotitolo: `TIER_COPY[tier].body`; CTA finale `PrimaryButton "Torna a Oggi"` → `reset(); router.replace("/(app)/today")` (comportamento attuale). La sessione di cartella (mode single + folderKind) usa titolo "Ripasso di {folder} completato!" — prendi il nome dalla prima entry? No: usa copy neutro "Sessione completata!" quando `mode === "single"`.

- [ ] **Step 4: Lint + verifica manuale + commit**

Run: `npm run lint` → exit 0. Verifica in Expo Go: sessione demo → recap con mascotte animata, barre, lista.

```bash
git add app/review/complete.tsx
git commit -m "feat(review): animated mascot session recap — chart, per-card outcomes, encouragement tiers"
```

---

### Task 8: `MascotLoader` + sostituzione degli spinner

**Files:**
- Create: `components/MascotLoader.tsx`
- Modify: `app/(app)/knowledge.tsx:84-88`, `app/(app)/folder/[kind].tsx:77-86`, `app/review/scan.tsx` + `reinforcement.tsx` + `focus.tsx` (lo stato `deckLoading` del Task 5), `app/folder-settings.tsx` (stato loading)

**Interfaces:**
- Produces: `MascotLoader({ label?: string; size?: number })` — mascotte oscillante centrata con etichetta opzionale.

- [ ] **Step 1: componente**

```tsx
// components/MascotLoader.tsx
import { useEffect } from "react";
import { Text, View } from "react-native";
import Animated, {
  useAnimatedStyle, useSharedValue, withRepeat, withSequence, withTiming,
} from "react-native-reanimated";
import { Mascot } from "@/components/Mascot";
import { FONT, colors } from "@/theme/tokens";

type Props = { label?: string; size?: number };

/**
 * Loader brandizzato: la mascotte oscilla dolcemente al posto degli
 * ActivityIndicator (spec §10 — "caricamenti carini con la mascotte").
 */
export function MascotLoader({ label, size = 84 }: Props) {
  const sway = useSharedValue(0);
  useEffect(() => {
    sway.value = withRepeat(
      withSequence(withTiming(1, { duration: 520 }), withTiming(-1, { duration: 520 })),
      -1, true,
    );
  }, [sway]);
  const style = useAnimatedStyle(() => ({
    transform: [{ rotate: `${sway.value * 8}deg` }, { translateY: Math.abs(sway.value) * -4 }],
  }));
  return (
    <View style={{ alignItems: "center", gap: 12 }}>
      <Animated.View style={style}>
        <Mascot variant="default" size={size} withShadow={false} />
      </Animated.View>
      {label ? (
        <Text style={{ fontFamily: FONT.regular, fontSize: 13.5, color: colors.midGrey }}>
          {label}
        </Text>
      ) : null}
    </View>
  );
}
```

- [ ] **Step 2: sostituzioni**

- `knowledge.tsx` (empty/loading): `<ActivityIndicator color={colors.navy} />` → `<MascotLoader label="Carico le tue cartelle…" />`; rimuovi `ActivityIndicator` dall'import RN.
- `folder/[kind].tsx` (loading): → `<MascotLoader label="Apro la cartella…" />` centrato; rimuovi import.
- Tre screen review (`deckLoading` del Task 5): → `<MascotLoader label="Preparo il ripasso…" />`.
- `folder-settings.tsx`: mentre `loading && !folder`, mostra `<MascotLoader label="Un attimo…" />` sotto la TopBar.

- [ ] **Step 3: Lint + commit**

Run: `npm run lint` → exit 0; `grep -rn "ActivityIndicator" app` → nessun risultato.

```bash
git add components/MascotLoader.tsx app
git commit -m "feat(ui): MascotLoader — branded loading state app-wide"
```

---

### Task 9: Add — campi separati + salvataggio vero

**Files:**
- Modify: `app/add.tsx`

**Interfaces:**
- Consumes: `createMemory`, `fetchTodayInputCount`, `fetchProfile` (api); `useFoldersWithStats` (per `folder.id`); `DAILY_INPUT_CAP_DEFAULT` da constants.

- [ ] **Step 1: stato dei campi**

Sostituisci `const [text, setText] = useState("")` con:

```ts
const [term, setTerm] = useState("");
const [reading, setReading] = useState("");
const [definition, setDefinition] = useState("");
const [example, setExample] = useState("");
const [saving, setSaving] = useState(false);
```

e il daily count finto con:

```ts
const [dailyCount, setDailyCount] = useState<number | null>(null);
const [dailyMax, setDailyMax] = useState(DAILY_INPUT_CAP_DEFAULT);
useEffect(() => {
  if (!user) return;
  let cancelled = false;
  Promise.all([fetchTodayInputCount(user.id), fetchProfile(user.id)])
    .then(([count, profile]) => {
      if (cancelled) return;
      setDailyCount(count);
      if (profile) setDailyMax(profile.dailyInputCap);
    })
    .catch((e) => { if (__DEV__) console.warn("[add] daily count load failed", e); });
  return () => { cancelled = true; };
}, [user]);
const dailyLimitReached = (dailyCount ?? 0) >= dailyMax;
const canSave = term.trim().length > 0 && definition.trim().length > 0 && !saving && !dailyLimitReached;
```

(la sorgente cartelle: sostituisci l'attuale lista con `useFoldersWithStats()` se `folders` non espone già `id` — serve `folder.id` per il salvataggio).

- [ ] **Step 2: form UI**

Al posto della textarea unica, quattro input nello stesso stile della textarea attuale (bg-surface, radius 14, hairline): **Termine** (fontSize 18 semibold, placeholder "Termine da ricordare"), **Lettura** solo se `folder === "jp"` (placeholder "Lettura (opzionale) — es. muzukashii"), **Definizione** multiline minHeight 90 (placeholder "Cosa significa?"), **Esempio** multiline minHeight 70 (placeholder "Frase d'esempio (opzionale)"). Gap 10 tra i campi. L'anteprima card: Fronte = `term.trim() || preview.front`, sotto la lettura se presente; Retro = `definition.trim() || preview.back`; se `example` è compilato, mostralo in corsivo sotto il retro.

- [ ] **Step 3: doSave vero**

```ts
const doSave = async (addAnother: boolean) => {
  if (!canSave || !user) return;
  const folderRow = folders.find((f) => f.kind === folder);
  if (!folderRow) return;
  setSaving(true);
  try {
    await createMemory({
      userId: user.id,
      folderId: folderRow.id,
      term: term.trim(),
      reading: folder === "jp" && reading.trim() ? reading.trim() : undefined,
      definition: definition.trim(),
      example: example.trim() ? example.trim() : undefined,
      itemType: type,
    });
    setDailyCount((c) => (c ?? 0) + 1);
    showToast(`Salvato in ${folderRow.name} · primo ripasso domani`);
    if (addAnother) {
      setTerm(""); setReading(""); setDefinition(""); setExample("");
    } else {
      safeBack("/(app)/knowledge");
    }
  } catch (e) {
    if (__DEV__) console.warn("[add] save failed", e);
    showToast("Salvataggio non riuscito. Riprova.");
  } finally {
    setSaving(false);
  }
};
```

Contatore in basso: mostra `…` finché `dailyCount === null`, poi `${dailyCount} / ${dailyMax} ricordi oggi` (logica limite invariata). I tre bottoni Salva usano `disabled={!canSave}` come oggi.

- [ ] **Step 4: Lint + verifica manuale + commit**

Run: `npm run lint` → exit 0. In Expo Go (account reale, non demo): salva un ricordo → verifica riga in Supabase:
```bash
curl -X POST -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" -H "Content-Type: application/json" \
  -d '{"query": "select term, definition, next_review_at from public.memories order by created_at desc limit 3"}' \
  https://api.supabase.com/v1/projects/taekvxxljtgzsjrlmumo/database/query
```

```bash
git add app/add.tsx
git commit -m "feat(add): separate term/reading/definition/example fields with real persistence and daily cap"
```

---

### Task 10: Oggi — piano giornaliero reattivo al budget

**Files:**
- Modify: `app/(app)/today.tsx`

**Interfaces:**
- Consumes: `fetchDueCounts` (Task 3); `splitBudget`, `layerMinutes`, `totalMinutes`, `type LayerCounts` (Task 2); `TIME_BUDGETS`; AsyncStorage.

- [ ] **Step 1: budget persistito + conteggi al focus**

```ts
const BUDGET_KEY = "memika.time-budget-minutes";
const [budget, setBudget] = useState(15);
useEffect(() => {
  AsyncStorage.getItem(BUDGET_KEY).then((v) => {
    const n = v ? Number(v) : NaN;
    if (TIME_BUDGETS.some((b) => b.minutes === n)) setBudget(n);
  }).catch(() => {});
}, []);
const pickBudget = (minutes: number) => {
  setBudget(minutes);
  AsyncStorage.setItem(BUDGET_KEY, String(minutes)).catch(() => {});
};

const [dueCounts, setDueCounts] = useState<LayerCounts | null>(null);
useFocusEffect(useCallback(() => {
  if (!user) return;
  let cancelled = false;
  fetchDueCounts(user.id)
    .then((c) => { if (!cancelled) setDueCounts(c); })
    .catch((e) => { if (__DEV__) console.warn("[today] due counts failed", e); });
  return () => { cancelled = true; };
}, [user]));
```

(`user` da `useAuthStore((s) => s.user)` — sostituisce la selezione attuale del solo nome.)

- [ ] **Step 2: piano derivato**

Sostituisci la costante modulo `PLAN`/`TOTAL_*` con derivazione in-component:

```ts
const estItems = TIME_BUDGETS.find((b) => b.minutes === budget)?.estItems ?? 28;
const counts = dueCounts ?? DEMO_DUE_COUNTS; // fallback pre-fetch: mai NaN in UI
const plan = splitBudget(counts, estItems);
const planMinutes = {
  scan: layerMinutes("scan", plan.scan),
  reinforcement: layerMinutes("reinforcement", plan.reinforcement),
  focus: layerMinutes("focus", plan.focus),
};
const totItems = plan.scan + plan.reinforcement + plan.focus;
const totMin = totalMinutes(plan);
```

LayerCard: `items={plan.scan}` etc.; sottotitoli: `` `Ricordi più vecchi · ~${planMinutes.scan} min` `` (stessa forma per gli altri due, testi attuali). Riga totale: `Totale · {totItems} ricordi · circa {totMin} min`. CTA: se `totItems === 0`, `PrimaryButton` disabilitato con label "Niente da ripassare ora"; altrimenti "Inizia il ripasso di oggi". `TimeBudgetChips value={budget} onChange={pickBudget}`.

- [ ] **Step 3: avvio sessioni col budget**

`startReview`: `startSession("scan", "flow", { budgetCap: estItems }); router.push("/review/scan");`
`startLayer(path)`: `startSession(path, "single", { budgetCap: estItems }); router.push(...)` (livello singolo = tutto il budget su quel livello, per scelta di spec).

- [ ] **Step 4: Lint + verifica manuale + commit**

Run: `npm run lint` → exit 0. In Expo Go: cambia chip → card e totale si aggiornano subito; riavvia l'app → il chip resta.

```bash
git add "app/(app)/today.tsx"
git commit -m "feat(today): budget-reactive daily plan with persisted time budget"
```

---

### Task 11: Progressi con dati veri

**Files:**
- Modify: `app/(app)/health.tsx`

**Interfaces:**
- Consumes: `useFoldersWithStats`; `fetchDueCounts`; `layerMinutes` non serve qui.

- [ ] **Step 1: righe cartella reali**

Sostituisci le quattro `<HealthRow name="Japanese" …/>` hardcoded con:

```tsx
{orderedFolders.map((f) => (
  <HealthRow
    key={f.kind}
    name={f.name}
    active={f.active}
    fading={f.fading}
    archived={f.archived}
    chip={f.active >= 70 ? "Alta" : f.active >= 45 ? "Media" : "Bassa"}
  />
))}
```

dove `const { folders } = useFoldersWithStats();` e `orderedFolders = applyFolderOrder(folders, order)` (stesso pattern di knowledge.tsx — import `useFolderOrderStore`/`applyFolderOrder`). Cartelle in pausa: mostra comunque la riga (lo stato di salute resta informativo).

- [ ] **Step 2: insight "Riequilibra" onesto**

```ts
const worst = useMemo(() => {
  let best: { f: FolderWithStats; fadingCount: number } | null = null;
  for (const f of folders) {
    const fadingCount = Math.round((f.count * f.fading) / 100);
    if (fadingCount > 0 && (!best || fadingCount > best.fadingCount)) best = { f, fadingCount };
  }
  return best;
}, [folders]);
```

Se `worst === null` → non renderizzare la card insight. Altrimenti testo: `` `${worst.f.name} ha ${worst.fadingCount} ricordi in dissolvenza — una sessione mirata li può recuperare.` `` e link `router.push({ pathname: "/folder/[kind]", params: { kind: worst.f.kind } })` con label "Riequilibra ora →" e accessibilityLabel coerente.

- [ ] **Step 3: carico cognitivo derivato**

```ts
const [due, setDue] = useState<LayerCounts | null>(null);
useFocusEffect(useCallback(() => {
  if (!user) return;
  let cancelled = false;
  fetchDueCounts(user.id).then((c) => { if (!cancelled) setDue(c); }).catch(() => {});
  return () => { cancelled = true; };
}, [user]));
const dueTotal = due ? due.scan + due.reinforcement + due.focus : null;
// Euristica dichiarata (spec §8): 120 items ≈ tetto del budget 1h.
const loadPct = dueTotal === null ? 62 : Math.min(100, Math.round((dueTotal / 120) * 100));
```

`<CognitiveLoadBar pct={loadPct} />`. (62 resta solo come placeholder pre-fetch, stesso valore attuale.)

- [ ] **Step 4: Lint + commit**

Run: `npm run lint` → exit 0.

```bash
git add "app/(app)/health.tsx"
git commit -m "feat(health): real folder stats, honest rebalance insight, queue-derived cognitive load"
```

---

### Task 12: Impostazioni cartella complete + pausa visibile

**Files:**
- Modify: `app/folder-settings.tsx` (stats + pausa + elimina)
- Modify: `components/ActionPill.tsx` (prop `disabled`)
- Modify: `app/(app)/folder/[kind].tsx` ("Ripassa ora" disabilitato se in pausa)
- Modify: `components/FolderRow.tsx` (badge "In pausa" + attenuazione)
- Modify: `app/(app)/knowledge.tsx` (passa `paused` a FolderRow)

**Interfaces:**
- Consumes: `updateFolderPaused`, `deleteFolder`, `countMemoriesInFolder` (Task 3); `Folder.paused` (Task 1); `SettingsToggle` da `components/SettingsRow.tsx`; `RetentionBar`, `StatBlock`.

- [ ] **Step 1: blocco statistiche in folder-settings**

Sopra la sezione "Nome" (usa `folder` e `items` da `useFolderDetail` — aggiungi `items` alla destructure):

```tsx
<View style={{ paddingHorizontal: 24, paddingTop: 20, paddingBottom: 8 }}>
  <SectionLabel>Statistiche</SectionLabel>
</View>
<View style={{ paddingHorizontal: 16 }}>
  <View className="rounded-card bg-surface" style={{ paddingHorizontal: 18, paddingTop: 18, paddingBottom: 16, borderWidth: 1, borderColor: colors.hairline }}>
    <RetentionBar active={folder?.active ?? 0} fading={folder?.fading ?? 0} archived={folder?.archived ?? 0} width="100%" height={10} />
    <View className="mt-4 flex-row" style={{ justifyContent: "space-between" }}>
      <StatBlock dot={colors.active} label="Stabili" pct={folder?.active ?? 0} count={Math.round(((folder?.count ?? 0) * (folder?.active ?? 0)) / 100)} />
      <StatBlock dot={colors.fading} label="In dissolvenza" pct={folder?.fading ?? 0} count={Math.round(((folder?.count ?? 0) * (folder?.fading ?? 0)) / 100)} />
      <StatBlock dot={colors.archived} label="Archiviati" pct={folder?.archived ?? 0} count={Math.round(((folder?.count ?? 0) * (folder?.archived ?? 0)) / 100)} />
    </View>
    <Text style={{ marginTop: 14, fontFamily: FONT.regular, fontSize: 12.5, color: colors.midGrey, fontVariant: ["tabular-nums"] }}>
      {folder?.count ?? 0} ricordi · {folder?.addedThisWeek ?? 0} aggiunti questa settimana · ultimo ripasso {lastReviewLabel}
    </Text>
  </View>
</View>
```

con `const lastReviewLabel = useMemo(() => { const ts = items.map((i) => i.lastReviewedAt).filter(Boolean).sort().at(-1); return ts ? relativeReviewed(ts) : "mai"; }, [items]);` (import `relativeReviewed` da `@/lib/format`).

- [ ] **Step 2: toggle pausa**

Sezione dopo "Nome":

```tsx
<View style={{ paddingHorizontal: 24, paddingTop: 24, paddingBottom: 8 }}>
  <SectionLabel>Ritmo</SectionLabel>
</View>
<View style={{ paddingHorizontal: 16 }}>
  <SettingsToggle
    key={folder ? `paused-${folder.paused}` : "paused"}
    label="Metti in pausa"
    hint="La cartella esce dai ripassi finché non la riattivi. Nessun ricordo va perso."
    defaultOn={folder?.paused ?? false}
    onChange={(v) => {
      if (!folder) return;
      updateFolderPaused(folder.id, v)
        .then(() => { showToast(v ? "Cartella in pausa" : "Cartella riattivata"); refetch(); })
        .catch((e) => { if (__DEV__) console.warn("[folder] pause failed", e); showToast("Operazione non riuscita. Riprova."); });
    }}
  />
</View>
```

(aggiungi `refetch` alla destructure di `useFolderDetail`).

- [ ] **Step 3: eliminazione con conferma**

Stato: `const [confirmDelete, setConfirmDelete] = useState(false); const [memoryCount, setMemoryCount] = useState<number | null>(null); const [deleting, setDeleting] = useState(false);`

Bottone in fondo alla pagina (sotto il PrimaryButton Salva, marginTop 28): Tappable testuale rosso (`colors.danger`) "Elimina cartella" → `onPress: async () => { if (folder) setMemoryCount(await countMemoriesInFolder(folder.id).catch(() => null)); setConfirmDelete(true); }`.

Modal di conferma (RN `Modal transparent animationType="fade"`, card centrata bg-warm-white radius 18, pattern del delete-account in settings.tsx):
- Titolo: "Eliminare {folder.name}?"
- Corpo: `` {memoryCount === null ? "Tutti i ricordi della cartella" : `${memoryCount} ricordi`} verranno eliminati per sempre. Non si può annullare. ``
- Bottoni: GhostButton "Annulla" → `setConfirmDelete(false)`; PrimaryButton rosso "Elimina" (disabled={deleting}) →

```ts
const handleDelete = async () => {
  if (!folder) return;
  setDeleting(true);
  try {
    await deleteFolder(folder.id);
    showToast(`Cartella ${folder.name} eliminata`);
    router.replace("/(app)/knowledge");
  } catch (e) {
    if (__DEV__) console.warn("[folder] delete failed", e);
    showToast("Eliminazione non riuscita. Riprova.");
    setDeleting(false);
  }
};
```

(import `router` da expo-router in folder-settings.tsx.)

- [ ] **Step 4: pausa visibile in cartella e in lista**

- `components/ActionPill.tsx`: aggiungi prop `disabled?: boolean`; passa `disabled` al Tappable e applica `opacity: disabled ? 0.45 : 1` al contenuto (o via containerStyle).
- `app/(app)/folder/[kind].tsx`: `<ActionPill icon={Repeat} label="Ripassa ora" … disabled={data.paused} />`; sotto le due pill, se `data.paused`: `<Text style={{ paddingHorizontal: 22, paddingTop: 8, fontFamily: FONT.regular, fontSize: 12.5, color: colors.midGrey }}>Cartella in pausa — riattivala dalle impostazioni per ripassarla.</Text>`.
- `components/FolderRow.tsx`: prop `paused?: boolean`; quando true, avvolgi il contenuto con `opacity: 0.55` e accanto al nome un badge `<View style={{ backgroundColor: colors.divider, paddingHorizontal: 6, paddingVertical: 2, borderRadius: 6 }}><Text style={{ fontFamily: FONT.semibold, fontSize: 10, color: colors.midGrey }}>In pausa</Text></View>` (adatta al layout reale del file — leggi FolderRow prima di editare).
- `knowledge.tsx` renderItem: `paused={item.paused}`.

- [ ] **Step 5: Lint + commit**

Run: `npm run lint` → exit 0.

```bash
git add app/folder-settings.tsx components/ActionPill.tsx components/FolderRow.tsx "app/(app)/folder/[kind].tsx" "app/(app)/knowledge.tsx"
git commit -m "feat(folders): folder settings — stats, pause toggle, delete with confirm; paused state visible"
```

---

### Task 13: Verifica finale + docs

**Files:**
- Modify: `docs/ROUTING.md` (rotta `/folder-settings` se non già documentata), `docs/ROADMAP.md` (spuntare i punti Fase 3 completati)

- [ ] **Step 1: suite completa**

Run: `npm test` → tutti verdi. Run: `npm run lint` → exit 0.

- [ ] **Step 2: E2E manuale su device (account reale)**

Checklist della spec §12, in ordine: aggiungi ricordo → appare in Scan → flusso fluido senza tap → recap animato con la carta appena aggiunta → cambia budget → piano cambia → pausa cartella → sparisce dalla coda → elimina cartella di prova → Progressi coerente. Poi passa a demo mode e verifica che tutto renda come prima.

- [ ] **Step 3: docs + commit finale**

```bash
git add docs
git commit -m "docs: routing + roadmap updates for core-loop cycle"
```

---

## Note per l'esecutore

- I task 5–8 dipendono dal 4; il 4 dipende da 2+3; il 9–12 dipendono da 1–3 ma sono indipendenti tra loro (parallelizzabili con worktree se si usa subagent-driven).
- Dove il piano dice "leggi il file prima di editare" (reinforcement stage, FolderRow), l'anchor esatto va verificato sul file reale — la logica richiesta è specificata sopra.
- Ogni stringa utente è in italiano, tono calmo. Mai aggiungere copy colpevolizzante.
